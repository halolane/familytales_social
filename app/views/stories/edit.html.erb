<h1>Editing story</h1>

<%= render 'form' %>

<%= link_to 'Show', @story %> |
<%= link_to 'Back', stories_path %>

<script type="text/javascript">
             tinymce.init({
                selector: "div.mom_reply",
                theme: "modern",
                plugins: [
                    ["autolink link image charmap preview hr anchor spellchecker"],
                    ["wordcount insertdatetime media"],
                    ["save contextmenu directionality paste"]
                ],
                add_unload_trigger: false,
                schema: "html5",
                inline: true,
                toolbar: "undo redo | bold italic | alignleft aligncenter alignright | link image | media",
                statusbar: false,
                menubar: false
            });

            tinymce.init({
                selector: "h1.question",
                theme: "modern",
                add_unload_trigger: false,
                schema: "html5",
                inline: true,
                toolbar: "undo redo",
                statusbar: false,
                menubar: false
            });

            tinymce.init({
                selector: "figcaption",
                theme: "modern",
                add_unload_trigger: false,
                schema: "html5",
                inline: true,
                toolbar: "undo redo",
                statusbar: false,
                menubar: false
            });
        </script>